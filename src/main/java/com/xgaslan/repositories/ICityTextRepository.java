package com.xgaslan.repositories;

import org.springframework.stereotype.Repository;

@Repository
public interface ICityTextRepository {
    // Define methods for interacting with city text data
    // For example:
    // List<CityText> findAll();
    // CityText findById(Long id);
    // void save(CityText cityText);
    // void deleteById(Long id);{
}
